{
  "outcome": "pass",
  "kind": "validator",
  "note": "Parameters with ranges/enums plus a declared ${setting.*} template; profiles are gone and operator launch settings live in the controller database."
}
